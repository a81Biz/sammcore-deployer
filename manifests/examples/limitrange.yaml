apiVersion: v1
kind: LimitRange
metadata:
  name: project-limits
spec:
  limits:
    - default:
        cpu: "500m"
        memory: "512Mi"
      defaultRequest:
        cpu: "50m"
        memory: "64Mi"
      type: Container
